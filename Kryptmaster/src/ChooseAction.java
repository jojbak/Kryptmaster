


/**
 * Chooses which action should be taken depending on which button is pressed
 * in the GUI.
 * @author jonathan
 *
 */
public interface ChooseAction {

	/**
	 * Calls on a certain method depending on which button the user presses
	 */
	//boolean chooseAction(Algorithm alg, String key, Input in);
}
